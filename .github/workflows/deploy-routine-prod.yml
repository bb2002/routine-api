# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Development

on:
  push:
    branches:
      - trunk
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: npm install, build, and test
        run: |
          npm ci
          npx prisma generate
          npm run build --if-present
          npm run test --if-present

      # - name: Generate prisma/crt.pem
      #   env:
      #     DATABASE_CERT: ${{ secrets.DATABASE_CERT }}
      #   run: |
      #     touch prisma/crt.pem
      #     echo $DATABASE_CERT > prisma/crt.pem
      #     echo "ls -al prisma/"
      #     ls -al prisma/

      # - name: Zip artifact for deployment
      #   run: zip release.zip ./* -r

      # - name: 'Deploy to Azure Web App'
      #   id: deploy-to-webapp
      #   uses: azure/webapps-deploy@v2
      #   with:
      #     app-name: 'app-routine-prod'
      #     slot-name: 'Production'
      #     package: .
      #     publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_A2A17D4185864A5FA699D05D2D25AB78 }}

